export function Footer() {
  return (
    <footer className="border-t border-line px-4 py-8 sm:px-6">
      <div className="mx-auto flex max-w-6xl flex-col gap-2 font-mono text-xs text-ink-faint sm:flex-row sm:items-center sm:justify-between">
        <p>Go build something that moves.</p>
        <p>Progress is stored only in this browser. Export it before you clear your data.</p>
      </div>
    </footer>
  )
}
